spring:
  config:
    activate:
      on-profile: development
  datasource:
    url: jdbc:h2:mem:devdb
    driverClassName: org.h2.Driver
    username: admin
    password: password123
  security:
    user:
      name: admin
      password: password123
jpa:
  hibernate:
  ddl-auto: update