logging:
  level:
    root: WARN
    org:
      springframework:
        security: DEBUG

spring:
  application:
    name: app
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create
  profiles:
    active: development

  output:
    ansi:
      enabled: always

security.enable-csrf: false

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    api-docs:
      path: /api-docs

sentry:
  dsn: https://a87f816791394a7f7acaa20297d11835@o4510864168714240.ingest.de.sentry.io/4510864300245072
  send-default-pii: true

rsa:
  private-key: classpath:certs/private.pem
  public-key: classpath:certs/public.pem